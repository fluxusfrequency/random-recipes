<div class="row">
  <h1 class="col-xs-12">Listing recipes</h1>

  <div class="col-xs-12">
    <table>
      <thead>
        <tr>
          <th>Name</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @recipes.each do |recipe| %>
          <tr>
            <td><%= recipe.name %></td>
            <% if recipe.notes && recipe.notes.match(/http/) %>
              <td><%= link_to 'show link', recipe.notes %></td>
            <% else %>
              <td></td>
            <% end %>
            <td><%= link_to 'Show', recipe %></td>
            <td><%= link_to 'Edit', edit_recipe_path(recipe) %></td>
            <td><%= link_to 'Destroy', recipe, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>

  <hr />
  <hr />

  <div class="col-xs-6 recipe-actions">
    <%= button_to 'New Recipe', new_recipe_path, class: 'btn btn-success' %>
    <%= button_to 'Random Recipe', root_path, class: 'btn btn-warning' %>
  </div>
</div>
